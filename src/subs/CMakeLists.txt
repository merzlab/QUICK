# NOTE: the source in this folder is handled by the CMakeLists.txt in the parent folder

#file(GLOB subsrc "*.f90")
#foreach(src ${subsrc})
#	get_filename_component(filename ${src} NAME_WE)
#	set(filelist ${filelist} "${src}" CACHE INTERNAL "")
#endforeach()


